# <Project / scope name> — main design (implemented state)

<!-- The single source of truth for WHAT IS IMPLEMENTED. Living — amended at each bundle's
     fold so it always describes the shape on main right now. Never dated, never deleted,
     exempt from byte budgets. Present tense only: no history, no "previously…" prose — that
     is git's job. Facts owned by another doc (a rule, the project log) are linked, not
     restated. A completing slice's design.md merges in here.
     Contract: .claude/rules/04-docs.md §Bundle lifecycle & the main bundle. -->

**Scope:** <scope> · **Living** — the implemented-state authority.
**PRD:** [prd.md](./prd.md) · **Spec:** [spec.md](./spec.md) ·
**Plan:** [plan.md](./plan.md) · **Log:** [log.md](./log.md)

---

## Overview

<!-- What the system is and where it sits. -->

## Architecture

<!-- Components and seams as they exist: what owns what, who calls whom. Organize the rest
     of this file however the scope demands (by subsystem, by surface) — the section set
     below is a starting point, not a straitjacket. -->

## Contracts & schemas

<!-- The interfaces the implementation currently commits to. -->

## Known gaps (enforcement vs. intent)

<!-- Where the implementation lags the rules or the design — each with its tracking pointer
     (backlog item, issue). An honest gap list is what keeps this file trustworthy. -->
