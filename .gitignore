.claude/.pr-review-loop.state.json

# Nested project repos live under projects/ — each is its own git repo, so the
# container is ignored wholesale rather than committed as a gitlink. This is the
# blessed dev-home layout (.claude/skills/pr-round/SKILL.md § Deployment).
/projects/

# Docusaurus site artifacts
site/node_modules/
site/build/
site/.docusaurus/

# Brainstorming visual-companion scratch
.superpowers/

# Playwright MCP scratch (snapshots, screenshots)
.playwright-mcp/

# Generated kickoff-deck renders (build from docs/workshops/building/kickoff-deck.md
# with tools/build_deck.py; binaries are never committed)
*.pptx
*.odp
